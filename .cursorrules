# 🤖 Cursor Rules - خبير البرومبتات

## 📋 معلومات المشروع
- **النوع**: موقع تعليمي تفاعلي (كتاب إلكتروني)
- **اللغة الطبيعية**: العربية (RTL)
- **Framework**: Next.js 14.2.0 (App Router)
- **اللغة البرمجية**: TypeScript
- **Styling**: Vanilla CSS (globals.css)
- **Animations**: Framer Motion v11
- **Backend**: Supabase (Auth + Database)

---

## 🎯 القواعد الأساسية

### اللغة والتنسيق
- استخدم اللغة العربية في جميع النصوص والتعليقات الموجهة للمستخدم
- اتجاه الكتابة RTL (Right-to-Left)
- استخدم خطوط Cairo للعناوين و Tajawal للنصوص

### الكود
- اكتب كود TypeScript نظيف ومُنظم
- استخدم `'use client'` للمكونات التي تحتاج JavaScript
- استخدم CSS Variables من globals.css
- لا تستخدم Tailwind CSS - المشروع يستخدم Vanilla CSS

### المكونات
- ضع المكونات في `/src/components/`
- مكونات المصادقة في `/src/components/auth/`
- مكونات القراءة في `/src/components/reading/`
- استخدم Framer Motion v11 للحركات

---

## 🎨 نظام التصميم

### الألوان الأساسية
```css
--color-bg-darker: #050505      /* خلفية أساسية */
--color-bg-dark: #0a0a0a        /* خلفية ثانوية */
--color-bg-card: #1a1a1a        /* خلفية البطاقات */
--color-orange-primary: #FF6B35  /* اللون الأساسي */
--color-orange-glow: #ff8c42    /* تأثير التوهج */
--color-text-primary: #FFFFFF   /* نص أساسي */
--color-text-secondary: #b0b0b0 /* نص ثانوي */
```

### المسافات
```css
--spacing-xs: 0.5rem   --spacing-sm: 1rem
--spacing-md: 1.5rem   --spacing-lg: 2rem
--spacing-xl: 3rem     --spacing-2xl: 4rem
```

### الأزرار
- استخدم `.btn-primary` للأزرار الرئيسية
- استخدم `.btn-secondary` للأزرار الثانوية

---

## 🔐 نظام المصادقة

### الملفات الأساسية
- `auth_system.ts` - النظام الكامل
- `auth.ts` - التحقق السريع (client-side)
- `fingerprint.ts` - بصمة الجهاز
- `cookie_utils.ts` - إدارة الكوكيز

### التحقق من الجلسة
```typescript
// تحقق سريع (client-side)
import { verifySession } from '@/lib/auth'
const isLoggedIn = verifySession()

// تحقق كامل (with database)
import { authSystem } from '@/lib/auth_system'
const result = await authSystem.verifySession()
```

---

## 📚 بيانات المحتوى

### الهيكل
```typescript
interface PageContent {
    id: number
    chapterNumber: number
    pageNumber: number
    title: string
    description: string
    contentBlocks: ContentBlock[]
}
```

### الأقسام
- `introData` - المقدمة (2 صفحات)
- `section1` - `section6` - الأقسام الرئيسية
- `libraryData` - مكتبة القوالب
- `appendixData` - الملحق
- `glossaryData` - المعجم

---

## 📁 هيكل الملفات المهمة

```
src/
├── app/                    # صفحات Next.js
│   ├── layout.tsx         # التخطيط الرئيسي
│   ├── page.tsx           # الصفحة الرئيسية
│   └── read/[section]/[page]/ # صفحات المحتوى
├── components/            # المكونات
├── data/bookData.ts       # بيانات الكتاب
└── lib/                   # المكتبات
    ├── auth_system.ts     # نظام المصادقة
    └── supabase.ts        # اتصال Supabase
```

---

## ⚠️ ملاحظات مهمة

1. **Framer Motion**: استخدم الإصدار 11 فقط
2. **Supabase**: المشروع يستخدم نظام مصادقة مخصص، وليس Supabase Auth الافتراضي
3. **الصفحات المقفلة**: أول صفحتين من كل قسم مجانية، الباقي يتطلب تسجيل دخول
4. **بصمة الجهاز**: تستخدم للحماية من الاستخدام المتعدد
5. **اقرأ**: PROJECT_MEMORY.md للتفاصيل الكاملة

---

## 🔄 عند إضافة ميزة جديدة

1. اقرأ الملف المناسب من المكونات الموجودة
2. اتبع نفس نمط الكود والتنسيق
3. استخدم CSS Variables من globals.css
4. أضف التعليقات بالعربية للنصوص الموجهة للمستخدم
5. حدّث PROJECT_MEMORY.md إذا كان التغيير كبيراً
